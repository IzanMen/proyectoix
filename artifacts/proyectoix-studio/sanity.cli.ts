import { defineCliConfig } from "sanity/cli";

export default defineCliConfig({
  api: {
    projectId: "jhzj7eh3",
    dataset: "production",
  },
  studioHost: "proyectoix",
  autoUpdates: false,
});
