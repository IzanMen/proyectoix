{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "bash ../scripts/with-node-version.sh tsx ./src/hello.ts",
    "typecheck": "bash ../scripts/with-node-version.sh tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
